# Copy to .env and adjust. API_BASE_URL = your machine IP when running backend.
# Use the SAME value as careGiverConnect .env.local ADMIN_SESSION_SECRET so admin API calls work.
EXPO_PUBLIC_ADMIN_SESSION_SECRET=dev-admin-secret
